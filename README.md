## How to use these files as links



`https://leomav.github.io/demo-assets` + `/${dir}` + `/{filename}.glb`

For example we need a model from `/april` directory

`https://leomav.github.io/demo-assets/april/church2.glb`
